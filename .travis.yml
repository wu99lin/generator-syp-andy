language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-syp-andy' ]; then cd .. && eval "mv $currentfolder generator-syp-andy" && cd generator-syp-andy; fi

